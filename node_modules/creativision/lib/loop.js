require('./req');
require('./res');

//注册全局流程
o2c.runtime.loop = function(req,res){
  //内建API接口获取
  var req = o2c.runtime.req(req);
  var res = o2c.runtime.res(res);
  //增加一个引用
  res._req = req;
  res.status(200);
  res.end('123');
}